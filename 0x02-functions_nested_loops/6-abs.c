#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @r: compiled variable
 *
 * Return: absolute value of integer
 */
int _abs(int r)
{
	if (r > 0)
	{
		return (r);
	}
	else
	{
	return (-r);
	}
	return (0);
}

